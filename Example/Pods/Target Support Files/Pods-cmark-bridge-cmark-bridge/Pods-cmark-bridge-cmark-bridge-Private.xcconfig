#include "Pods-cmark-bridge-cmark-bridge.xcconfig"
GCC_PREPROCESSOR_DEFINITIONS = $(inherited) COCOAPODS=1
HEADER_SEARCH_PATHS = "${PODS_ROOT}/Headers/Build" "${PODS_ROOT}/Headers/Build/cmark-bridge" "${PODS_ROOT}/Headers/Public" "${PODS_ROOT}/Headers/Public/cmark-bridge"
OTHER_LDFLAGS = -ObjC
PODS_ROOT = ${SRCROOT}